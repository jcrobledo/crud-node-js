<h1>Eliminar Producto</h1>
<form class="form-delete" action="/productos/<%= producto.id %>/delete?_method=DELETE" method="POST">
    <p class="p_form">
        <label for="id">ID:</label>
    </p>
    <input type="text" id="id" name="id" value="<%= producto.id %>" disabled />
    <p class="p_form">
        <label for="nombre">Nombre:</label>
    </p>
    <input type="text" id="nombre" name="nombre" value="<%= producto.title %>" disabled />
    <p class="p_form">
        <label for="descripcion">Descripción:</label>
    </p>
    <textarea id="descripcion" name="descripcion" disabled>
<%= producto.description %></textarea>
    <br>
    <br>
    <input type="submit" value="Eliminar Producto" />
</form>
<br>
<div align="center">
    <button onclick="location.href='/productos'">Volver a Productos</button>
</div>

<script>
    document.addEventListener('submit', (event) => {
        event.preventDefault();

        if (event.target.classList.contains('form-delete')) {
            if (confirm('¿Estás seguro de que deseas eliminar este producto?')) {
                event.target.submit();
            }
        }
    });  
</script>